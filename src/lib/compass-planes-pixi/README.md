## Add Editor Tool

- Add a new type to `ComponentType`
- Add an asset to `Editor Tools` called `<tool-name>-tool.png` that is 28px square
- Create a function that draws the button in `editor-decorators/create-component-menu/component-buttons`
- Call that function `drawComponentContainerMenu`
