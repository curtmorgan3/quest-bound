/** localStorage feature flag (see `@/utils/feature-flags`). When enabled, campaign realtime play rules apply when a session is active. */
export const CAMPAIGN_REALTIME_PLAY_FEATURE_FLAG = 'campaignRealtimePlay';
