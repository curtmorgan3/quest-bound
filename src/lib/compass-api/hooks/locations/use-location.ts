import type { Location } from '@/types';

/** Locations feature removed; stub always returns undefined. */
export const useLocation = (_locationId: string | undefined): Location | undefined => {
  return undefined;
};
