# SPA fallback: serve index.html for non-file routes so BrowserRouter works on refresh.
# Also mirrored in public/_redirects (copied into dist/) so deploys always pick it up.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200