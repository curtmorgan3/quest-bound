/**
 * Placeholder reference tab for the scripts page. Real QBScript documentation
 * will replace this stub.
 */

export function ReferenceTab() {
  return (
    <div className='p-4 flex flex-col gap-4 text-sm text-muted-foreground overflow-y-auto'>
      <h3 className='text-base font-semibold text-foreground'>QBScript reference</h3>
      <p>
        Reference documentation for QBScript will live here — context object members,
        lifecycle entry points (<code>on</code>, <code>hook</code>, <code>macro</code>,{' '}
        <code>validate</code>), dice expressions, and return-value contracts by script
        type.
      </p>
      <p className='italic'>Coming soon.</p>
    </div>
  );
}
